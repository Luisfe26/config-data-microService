server:
  port: 8002

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/ordersSales
    username: postgres
    password: 123456
  jpa:
    database: postgresql
    show-sql: false
    hibernate:
      ddl-auto: update